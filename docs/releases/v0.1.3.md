# yt2stems 0.1.3

## Fixed

- fixed `yt2stems-setup` for Homebrew installs where Homebrew relocates `LICENSE`, `README.md`, and `CHANGELOG.md` outside `libexec`
- updated the Homebrew formula to copy those metadata files back into `libexec` as a compatibility safeguard
