# yt2stems v0.1.2

Packaging consistency fix for the Homebrew bootstrap release.

## Highlights

- Bumped the packaged project version to `0.1.2` so the release artifact names, CLI `--version` output, and Git tag all line up
- Carries forward the Homebrew bootstrap layout fix from `v0.1.1`

## Upgrade notes

- Homebrew users should prefer `v0.1.2` and later because the packaged version metadata now matches the published tag
